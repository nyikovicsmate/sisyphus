/**
 * Generates a new patient id based on the supplied phone number, first checks if phone already exsists
 */

module.exports = function (objectRepository) {

    return function (req, res, next) {
        //TODO: implement
        return next();
    };

};