/**
 * Lists the subscribers from db
 */

module.exports = function (objectRepository, activeOnly) {

    return function (req, res, next) {
        //TODO: implement if subscriber model is available
        return next();
    };

};