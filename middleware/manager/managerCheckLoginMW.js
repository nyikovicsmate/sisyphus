/**
 * Checks if the given manager credentials match with the ones in the db, if so creates a new session
 */

module.exports = function (objectRepository) {

    return function (req, res, next) {
        //TODO: implement if user model is available
        return next();
    };

};