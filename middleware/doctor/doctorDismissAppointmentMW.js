/**
 * Dismisses a pending appointment if past due date or could not come to agreement regarding the date
 */

module.exports = function (objectRepository) {

    return function (req, res, next) {
        //TODO: implement if appointment model is available
        return next();
    };

};